enum TopicType {
  ATTACHED = 'attached',
  DETACHED = 'detached',
}

enum Direction {
  UP = 'up',
  DOWN = 'down',
  LEFT = 'left',
  RIGHT = 'right',
  NONE = 'none'
}

export {
  TopicType,
  Direction,
}